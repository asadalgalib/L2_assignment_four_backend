model Categories {
    id       String @id @default(uuid())
    tutorId  String
    tutor    User   @relation(fields: [tutorId], references: [id])
    category String @db.Text

    @@index([tutorId])
}
